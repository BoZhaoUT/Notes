export const environment = {
  production: false,
  navBarBackgroundColor: 'green'
};
// need to register this environment in .angular-cli.json
// this is an example of custom environment
// changes in custom will not be reflected immediate
// need to re-run ng serve --environment=test
